//! Whisper via **fast-whisper-burn** (Burn + WGPU). Artifacts live under
//! `~/.xos/models/transcription/burn/{tiny,small}/` and optionally the repo bundle
//! `src/core/ai/transcription/models/burn/`.

pub mod whisper;
pub mod whisper_ensure;
