[package]
name = "xos"
version = "0.1.23"
edition = "2021"
description = "Experimental OS Windows Manager"
authors = ["Dyllan McCreary <dyllan@xlate.ai>"]
license = "MIT"
default-run = "xos"

[dependencies]
burn = "0.20"
burn-backend = "0.20"
burn-wgpu = "0.20"
pixels = "0.15.0"
winit = "0.30.12"
winapi = { version = "0.3.9", features = ["winuser"] }
clap = { version = "4.4.10", features = ["derive"] }
cpal = "0.16.0"
dialoguer = "0.11"
tiny_http = "0.12"
webbrowser = "1.0.6"
once_cell = "1.19"
fontdue = "0.9.3"
delaunator = "1.0"
rustpython-vm = "0.3.0"
ringbuf = "0.3"
include_dir = "0.7"

# -------------------------------------------------------------------
# Non-WASM native dependencies
# -------------------------------------------------------------------
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
rand = "0.9.2"
getrandom = "0.3.4"
rand_pcg = "0.9.0"
rayon = "1.10"
image = { version = "0.25", features = ["default-formats"] }
rodio = { version = "0.21.1", features = ["mp3", "flac", "vorbis", "wav"] }
ctrlc = "3.4"

# -------------------------------------------------------------------
# Desktop-only dependencies (exclude iOS)
# -------------------------------------------------------------------
[target.'cfg(all(not(target_arch = "wasm32"), not(target_os = "ios")))'.dependencies]
nokhwa = { version = "0.10.7", features = ["input-native"] }
rfd = "0.16.0"

# -------------------------------------------------------------------
# Apple platforms → Metal backend automatically enabled
# -------------------------------------------------------------------
[target.'cfg(any(target_os = "macos", target_os = "ios"))'.dependencies]
metal = "0.32.0"
objc = "0.2"

# -------------------------------------------------------------------
# WebAssembly target
# -------------------------------------------------------------------
[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "0.2"
wasm-bindgen-futures = "0.4"
js-sys = "0.3"
web-sys = { version = "0.3", features = [
  "CanvasRenderingContext2d",
  "HtmlCanvasElement",
  "ImageData",
  "Document",
  "Window",
  "MouseEvent",
  "Navigator",
  "MediaDevices",
  "MediaStream",
  "MediaStreamConstraints",
  "HtmlVideoElement",
  "HtmlMediaElement",
  "TouchEvent",
  "Touch",
  "TouchList",
  "DomRect",
  "AudioContext",
  "AudioNode",
  "AudioDestinationNode",
  "AudioProcessingEvent",
  "ScriptProcessorNode",
  "MediaStreamAudioSourceNode",
  "console",
] }
console_error_panic_hook = "0.1"

[lib]
crate-type = ["cdylib", "rlib"]

[[bin]]
name = "xos"
path = "src/main.rs"

[workspace]
members = [".", "crates/xos-java"]
resolver = "2"

# gpu-allocator 0.27 allows windows 0.53–0.58; Cargo resolves 0.54 while wgpu-hal 26 uses 0.58, so DX12 types
# from two `windows` versions don't unify. Patch pins 0.27.0 to windows 0.58 (see patches/gpu-allocator/Cargo.toml).
[patch.crates-io]
gpu-allocator = { path = "patches/gpu-allocator" }
